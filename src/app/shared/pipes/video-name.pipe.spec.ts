import { VideoNamePipe } from './video-name.pipe';

describe('VideoNamePipe', () => {
  it('create an instance', () => {
    const pipe = new VideoNamePipe();
    expect(pipe).toBeTruthy();
  });
});
