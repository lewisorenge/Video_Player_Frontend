import { VideoLikesViewsPipe } from './video-likes-views.pipe';

describe('VideoLikesViewsPipe', () => {
  it('create an instance', () => {
    const pipe = new VideoLikesViewsPipe();
    expect(pipe).toBeTruthy();
  });
});
